// Scratch probe used while diagnosing the SWAT hunter GLB (bone span, node scales, bone
// names, texture count). Kept out of the check suite (underscore prefix, never run by the
// build). Safe to delete. The real, build-gating verification is tools/check-hunter-model-size.mjs.
