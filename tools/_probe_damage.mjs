// Inert stub. This was a one-off probe confirming the referee recomputes the size-based
// damage multiplier fresh on a re-disguise (small -> large). That behaviour is now covered
// permanently by tools/check-combat.mjs section E. Safe to delete (no shell in the sandbox).
console.log('superseded by tools/check-combat.mjs (section E: re-disguise multiplier)');
