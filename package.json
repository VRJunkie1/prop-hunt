{
  "name": "prop-hunt",
  "version": "0.1.0",
  "description": "Skeleton multiplayer prop hunt — browser client with an authoritative WebSocket referee server (no port forwarding required).",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18"
  }
}
