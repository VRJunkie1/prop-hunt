{
  "name": "prop-hunt",
  "version": "0.1.0",
  "description": "Skeleton multiplayer prop hunt — P2P WebRTC between browsers; a tiny Node matchmaker only mints room codes and relays the WebRTC handshake.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18"
  }
}
