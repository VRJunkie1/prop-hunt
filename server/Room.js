// TOMBSTONE — obsolete. Game logic lives in shared/referee.js (host browser).
// The whole `server/` directory is now dead (the matchmaker is retired too —
// signaling is PeerJS's public broker). Remove it with `git rm -r server`
// when a shell is available. See memory/project-state.md.
export {};
