// TOMBSTONE — obsolete. Config is content-as-data in shared/config/*.json,
// fetched by the client. The whole `server/` directory is now dead (the
// matchmaker is retired too — signaling is PeerJS's public broker). Remove it
// with `git rm -r server` when a shell is available. See memory/project-state.md.
export {};
